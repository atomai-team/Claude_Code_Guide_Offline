# Claude Code 小白使用手册
> CLI v2.1.175 · Desktop v1.9659.4 · OMC V52.2
> 从零开始，5分钟上手

---

## 🤔 Claude Code 是什么？

简单说：**把 Claude AI 直接放进你的终端/编辑器，让它帮你写代码、改 bug、做调研、管理项目**。

不是网页版对话框——它可以直接读你电脑上的文件、执行命令、写代码、运行测试，就像雇了个程序员坐在你旁边随时帮你干活。

加上 **OMC（oh-my-claudecode）** 这套增强系统，还多了 **128 专项技能、34 自动守护钩子**、可以同时派出多个 AI 并行工作。

---

## 🚀 安装和启动

### CLI（命令行版）

```bash
# 安装
npm install -g @anthropic-ai/claude-code

# 启动（在你的项目目录里）
cd ~/projects/my-project
claude
```

第一次启动会让你登录 Anthropic 账号，登录后就能用了。

### Desktop（桌面客户端版）

直接打开 Claude Code 桌面应用，点击 New Project 或者直接拖入项目文件夹。

### 两个版本的区别

| | CLI | Desktop |
|---|---|---|
| 适合 | 长任务、复杂项目、脚本自动化 | 快速问答、轻量改动、可视化 |
| 功能完整度 | 100% | ~70%（部分 hooks/插件不支持）|
| 推荐用法 | 主力工作 | 辅助/临时 |

---

## ⚡ 5分钟快速上手

### 第一步：进入项目目录

```bash
cd ~/projects/my-project
claude
```

### 第二步：跟 Claude 说话（就像聊天一样）

```
你：帮我看一下这个项目是做什么的

你：把 src/utils/helper.js 里的 formatDate 函数改成支持时区

你：现在报了一个错：TypeError: Cannot read property 'id' of undefined，帮我找一下哪里有问题

你：帮我给这个项目写一个 README
```

就是这么简单。不需要学特殊语法，说人话就行。

### 第三步：回应 Claude 的确认请求

Claude 在做某些操作（比如修改文件、执行命令）前会问你：

```
Allow this action?
> Yes (y) / No (n) / Always allow (a)
```

- 按 `Y` 或回车 = 允许这一次
- 按 `A` = 以后同类操作都允许
- 按 `N` = 不允许

嫌烦的话，可以说"**火力全开**"，之后就不再问了，全自动干活。

---

## 🧠 几个核心概念（用大白话理解）

### 会话（Session）
每次 `claude` 启动就是一个新会话，Claude 只记得这次对话里的内容。说 `/clear` 就是清空重开，换个话题时建议用。

### 技能（Skills）
可以理解为"特定领域的专家模式"。比如 `/code-review` 是代码审查专家，`/design` 是 UI 设计专家。用 `/技能名` 或者口语关键词触发。

### 钩子（Hooks）
在后台默默运行的守护程序，你不用管它。比如你一发消息，系统自动帮你检索相关历史记忆；你改了 settings.json，自动检测是不是有安全风险。

### Agents
当你说"队长，帮我做 XX"，Claude 会自动拆成多个任务，同时派出多个 AI 并行工作，最后汇总结果。速度是单线程的好几倍。

---

## 💬 最常用的口语说法（直接抄就能用）

### 开始一个任务

```
"帮我做一个商品详情页"
"给这个函数加单元测试"
"把登录功能重构一下，现在代码太乱了"
"帮我看看这个项目结构，然后给一个优化建议"
```

### 触发特殊模式

```
"队长，把整个购物车模块做完"        → 自动四步工作流，更细致、有验证
"火力全开，把剩下的 TODO 全做完"    → 极限并发，全自动不打断
"帮我调研一下 tRPC 和 REST 的区别"  → 自动深度调研多渠道
"做完之后做个全面的代码审查"        → 审查+修复
```

### 调试报错

```
"这个地方报了 404，帮我查一下"
"又报错了：[把报错信息粘贴进来]"
"反复出这个错，系统排查一下根因"
```

### 让 Claude 记住事情

```
"记住，这个项目用的是 PostgreSQL 14，不是 MySQL"
"记一下，API 超时时间统一设 30 秒"
```

### 撤回和修改

```
"这个方向不对，撤回去重新做"        → 说这句话或按 Esc Esc
"重新考虑一下，用另一种方式实现"
```

---

## 🛡️ 权限确认怎么回

Claude 修改文件或运行命令前，会有这样的提示：

```
◆ Allow bash command?
  ls -la src/
  Yes / No (always skip with "No")
```

**常见场景：**

| 情况 | 怎么回 |
|---|---|
| 正常操作，允许 | `Y` 或回车 |
| 这个不要执行 | `N` |
| 全部自动允许 | 说"**火力全开**"或提前说"**YOLO**" |
| 不确定，先看看 | `N`，然后问 Claude "你要执行什么，先说一下" |

---

## 📟 常用内置命令详解

### `/clear` — 清空重开
用法：直接说 `/clear`  
场景：换了个完全不相关的任务，或者对话太长太乱了  
效果：清空所有对话历史，上下文完全重置

---

### `/compact` — 压缩上下文
用法：`/compact`  
场景：对话已经很长了，Claude 开始忘前面说的事  
效果：把对话压缩成摘要，腾出空间继续

---

### `/model` — 切换模型
用法：`/model opus`  `/model sonnet`  `/model haiku`  
- opus：最强但慢，适合复杂架构/深度任务
- sonnet：平衡，日常主力
- haiku：最快，适合简单查询

---

### `/goal` — 设定任务完成标准
用法：`/goal "购物车可以正常加减商品并显示总价"`  
场景：长任务（做超过5轮的）开始前说，防止跑偏  
效果：Claude 会一直拿这个标准来检查自己做完没有

---

### `/btw` — 侧问（不影响主任务）
用法：`/btw 这个组件的名字叫什么？`  
场景：正在做一个大任务，突然想问个小问题  
效果：Claude 回答这个问题，但**不会把它算进主对话**，不污染上下文

---

### `/cd` — 切换工作目录
用法：`/cd ~/projects/backend`  
场景：同一个 session 里需要切到另一个项目  
效果：切换目录但保留对话历史和缓存（比退出重开省多了）

---

### `/safe-mode` — 纯净模式
用法：启动时加参数：`claude --safe-mode`  
场景：Claude 出现奇怪行为、hook 报错、插件冲突时排障  
效果：禁用所有 CLAUDE.md、插件、skills、hooks、MCP，用最干净的 Claude

---

### `/rewind` — 撤回
用法：`/rewind`  
场景：Claude 改了一堆代码，发现方向错了  
效果：回退到上一个检查点（Claude 做大操作前会自动打 checkpoint）

---

### `/code-review` — 代码审查
用法：
- `/code-review` — 只看问题，不修改
- `/code-review --fix` — 看完直接修复
- `/code-review --comment` — 看完发 PR 评论
- `/code-review ultra` — 云端多 agent 深度审查

---

## 🎯 如何调用技能（Skills）

### 方式1：直接说关键词（最自然）

```
"帮我调研一下..."           → 自动触发 deep-research
"全面升级一下配置"          → 触发 claude-code-better
"做个代码审查"              → 触发 code-review
"帮我做个安全审计"          → 触发 security-audit
"队长，全力以赴"            → 触发完整工作流
```

### 方式2：/ 命令触发

```
/code-review --fix
/design
/security-audit
/deploy-ship
/deep-research
/team
```

### 方式3：在说话里嵌入技能名

```
"用 autopilot 模式把这个功能做完"
"帮我 deep-dive 一下这个 bug 的根因"
"用 tdd 模式开发这个功能"
```

---

## 🔥 四大常用工作流程

### 流程1：修 Bug

```
1. 把报错信息直接粘给 Claude
   "这里报了：[报错内容]，帮我找一下"

2. Claude 会读相关文件，定位根因

3. 问 Claude：
   "你打算怎么改？先说方案"

4. 确认方案没问题后：
   "好，按这个改"

5. 改完问：
   "改完了，帮我验证一下这个场景还能正常跑"
```

### 流程2：开发新功能

```
1. 描述需求：
   "帮我做一个商品详情页，要有图片轮播、规格选择、加入购物车"

2. Claude 会先问一些关键问题（什么技术栈、什么框架等）

3. 确认技术细节后它开始写代码

4. 做完后说：
   "帮我做个代码审查"  （/code-review --fix）

5. 有问题自动修掉，没问题收工
```

### 流程3：做调研

```
1. 说：
   "帮我调研一下支付宝和微信支付接入，选哪个更合适，
    我们是 Node.js 后端，交易量初期不大"

2. Claude 会用 deep-research 多渠道调研

3. 给你一份有来源、有对比、有★推荐的报告

4. 你说："好，就按推荐方案来，给我接入教程"
```

### 流程4：全面审计/升级

```
1. 说：
   "帮我做一次全面的配置审计"  → 触发 /audit-all-better

2. 或者：
   "全面升级一下"              → 触发 /claude-code-better

3. 看报告，Claude 会给出 P0/P1/P2 分级的问题清单

4. P0 问题（严重）自动修，P1/P2 问你要不要改
```

---

## 🆘 遇到问题怎么办

### Claude 卡住不动了
按 `Esc Esc`（连按两次），中断当前操作。然后说说你想换个方向。

### Claude 方向跑偏了
```
"停一下，这个方向不对。我要的是 X，不是 Y。重新来"
```

### Claude 改了一堆错的东西
```
/rewind
```
或者说：`"撤回所有修改，回到你开始之前的状态"`

### 对话太长 Claude 开始犯傻
```
/compact
```
或者干脆 `/clear` 开新会话，把关键背景重新说一遍。

### Hook 报错、行为异常
```
claude --safe-mode
```
在纯净模式下复现问题，判断是配置问题还是代码问题。

### 想问一个问题但不想影响当前任务
```
/btw [你的问题]
```

---

## 🖥️ Desktop 客户端专属功能

### 设置全局 Instructions
**路径：Settings > General > "Instructions for Claude"**

这里可以写你希望 Claude 在所有对话里都遵守的规则，比如：
```
- 代码注释用中文
- 每次改完代码都要告诉我改了哪些文件
- 推荐时要给出至少 2 个备选方案
```

注意：这个作用于所有项目的对话，是全局生效的。

### 安装 MCP 扩展
**路径：Settings > Desktop app > Extensions**

MCP 是给 Claude 装"插件"的机制，让它能访问 GitHub、Notion、数据库等外部工具。  
Desktop 版的 MCP 必须通过 Extensions 页面安装，**不能手动改配置文件**。

### Cowork 专属 Instructions
**路径：Settings > Cowork**

只在 Cowork 协作模式下生效，不影响普通对话。

---

## 💡 实用小技巧

**1. 给 Claude 背景信息，让它更准**
```
"这个项目是 Next.js 14 + PostgreSQL，ORM 用 Prisma，
 部署在 Vercel。现在帮我..."
```
一开始说清楚技术栈，省去后面来回确认。

**2. 让 Claude 先说方案再动手**
```
"先告诉我你打算怎么实现，我确认了再做"
```
复杂功能必备，防止写了一大堆方向错的代码。

**3. 用 /btw 问不影响主任务的小问题**
正在做一个大功能，突然想确认一个细节：
```
/btw 咱们用的 React 版本是多少？
```
Claude 回答完继续做主任务，这个问题不会干扰它。

**4. 说"帮我验证一下"而不是自己以为没问题**
```
"功能写完了，帮我在几个典型场景下验证一下是否正常"
```

**5. 多窗口 / 多项目同时跑**
每个项目目录开一个终端，各自 `claude`，互不干扰。

**6. 长期项目的记忆技巧**
```
"记住：这个商城项目的数据库密码在 .env 里，
 部署到 Vercel，域名是 shop.example.com"
```
或者写进项目根目录的 CLAUDE.md 文件（用 `/init` 自动创建）。

---

## 📁 Desktop Settings 完整标签路径

```
Settings
├── General          ← 全局 Instructions 在这里 ★
├── Account
├── Privacy
├── Billing
├── Usage
├── Capabilities
├── Connectors
├── Claude Code
├── Cowork           ← Cowork 专属 Instructions 在这里
└── Desktop app
    ├── General
    ├── Extensions   ← MCP 扩展安装在这里 ★
    └── Developer
```

---

## 🎓 进阶：理解"四步工作流"

当你说"**队长**"，Claude 进入完整工作流：

1. **Plan（规划）** — 先理解需求，列出要改哪些文件、怎么改、验证方法
2. **Parallel（并行执行）** — 能同时做的任务自动并行，速度快几倍
3. **Force-Solve（强制解决）** — 遇到问题不放弃，最少尝试5种思路
4. **Verify（验证收口）** — 做完给实际运行证据，不说"应该可以了"

不说"队长"就是普通对话模式，适合简单任务。

---

---

## 🚀 进阶场景一：飞书集成实战

### 飞书 IM 机器人 + Claude Code 联动

**场景：团队消息触发 Claude 执行任务**

```
你：帮我写一个飞书机器人 webhook，接收消息后调用 Claude API 处理

Claude 需要知道：
1. 飞书应用 App ID / App Secret（从飞书开放平台获取）
2. 消息处理逻辑是什么
3. 回调地址怎么配
```

典型口语用法：
```
"帮我做一个飞书机器人，当 #产品讨论 群里有人@机器人时，
 自动分析需求并生成 PRD 草稿发回群里"

"飞书 Webhook 那边接收到表单提交后，
 自动整理数据发到指定群，帮我写这个处理器"

"帮我查一下飞书开放平台的最新 API 文档"
```

**飞书文档云 + Claude 知识库**

```
你：帮我把这份飞书文档（粘贴内容）整理成结构化的需求清单

你：读这份飞书多维表格导出的 CSV，帮我分析用户反馈的主要问题分类

你：把这次项目的所有决策记录整理后，帮我写入 Notion（或推送到飞书群）
```

**飞书审批流 + 自动化**

```
你：帮我写一个脚本：当飞书审批通过后，
    自动触发 GitHub Actions 部署

你：做一个飞书卡片消息模板，
    用于每日发送 Claude Code 任务执行报告
```

**MCP 快捷方式**（本机已配置 Gmail/Google Calendar，飞书类似原理）：
```
# 通过 MCP 直接操作（如已配置飞书 MCP）
"帮我查一下明天的飞书日程，有会议的话帮我准备会议材料"
"把今天的工作总结发给飞书 AI 助手频道"
```

---

## 🌍 进阶场景二：出海部署实战

### 海外技术栈选型

```
你：帮我调研一下，在东南亚部署 SaaS，
    选 AWS Singapore、GCP 还是 Azure？
    给我对比报告含延迟、价格、合规要求

Claude 会：用 deep-research 多渠道调研（官方定价+社区实测+合规文档），
给出★推荐方案，附对比表
```

### 国际化（i18n）开发

```
"帮我给这个商城项目加完整的 i18n 支持，
 需要支持中文、英文、马来语三种语言，
 用 next-i18next，所有文字都要提取出来"

"帮我把这个组件里的中文硬编码全部提取到 i18n 文件，
 并生成对应的英文翻译"

"帮我做一个 i18n 缺失检测脚本，
 找出 en 目录有但 zh 目录没有的 key"
```

### 多货币/多支付

```
"帮我集成 Stripe，支持 USD/SGD/MYR 三种货币，
 汇率实时更新，结算时自动换算"

"帮我做一个 PayPal + 本地支付（GrabPay/TNG）
 的统一抽象层，后端用 Node.js"
```

### 海外合规（GDPR/数据本地化）

```
"帮我做一个 GDPR 合规自查，
 看我们现在的用户数据处理哪里不合规"

"帮我添加 Cookie Banner，
 符合 GDPR 的数据同意要求，React 实现"

"帮我做数据导出功能，
 用户可以下载自己所有的数据（GDPR Art.20 数据可携带权）"
```

### 云端部署配置

```
"帮我写 Terraform，在 AWS Singapore 部署：
 - ECS Fargate（应用层）
 - RDS PostgreSQL（数据层）
 - CloudFront CDN（加速层）
 - WAF（安全层）"

"帮我做 GitHub Actions CI/CD，
 开发推送 → 自动测试 → 自动部署到 staging，
 打 tag → 自动发到 production"

"帮我配置 PM2 集群模式，
 充分利用多核 CPU，自动重启崩溃的进程"
```

---

## 👥 进阶场景三：专业创业团队协作交付

### 多角色并行开发

```
你：队长，我们团队3人并行开发商城，
    小李负责用户模块，小王负责商品模块，小张负责订单模块，
    帮我做好接口契约，让他们各自开发互不干扰

Claude 会：
1. 生成 OpenAPI/Swagger 接口规范
2. 建立 Mock Server
3. 写好集成测试方案
4. 约定分支策略和 PR 模板
```

### 产品需求 → 技术拆解

```
"我是 PM，这是我写的 PRD（粘贴进来），
 帮我生成：
 1. 开发任务清单（按模块拆分，含预估工时）
 2. 技术风险点
 3. 接口设计草稿
 4. 数据库设计草稿
 5. Linear 任务格式（直接同步到项目管理）"
```

### 代码审查 + PR 管理

```
"帮我审查这个 PR（粘贴 diff），
 重点看：安全漏洞、性能问题、逻辑错误、
 是否符合我们的代码规范"

"/code-review --comment"   ← 审查后直接把评论发到 GitHub PR

"帮我给团队制定代码规范文档，
 包含：命名规范、注释规范、Git commit 规范"
```

### 技术债务管理

```
"帮我扫描项目里的技术债务：
 1. TODO/FIXME 注释清单
 2. 循环依赖
 3. 重复代码
 4. 过时的依赖
 5. 安全漏洞
 给我一个优先级排序的修复计划"
```

### 团队知识库建设

```
"把这次的技术方案决策（ADR）整理成文档，
 说清楚：为什么这么选、放弃了什么方案、以后怎么改"

"帮我把这次 bug 的根因分析整理成故障复盘报告，
 给团队分享学习"

"用 /baoyu-slide-deck 把这次的架构设计做成演示文稿，
 给老板汇报用"
```

---

## 🎯 15 个 Claude 经典专业产出场景

### 一、技术产出类（5个）

**场景1：系统架构设计文档**
```
"帮我做一个电商系统的完整架构设计：
 - C4 模型（系统/容器/组件/代码四层）
 - 数据流图
 - 技术选型说明（含为什么不选 XXX）
 - 非功能性需求（性能/可用性/安全）
 - 架构决策记录（ADR）"
```
产出：20-30页专业架构文档

**场景2：全套 API 文档**
```
"根据这个 Express 项目，帮我生成：
 - OpenAPI 3.0 规范文件
 - Postman Collection
 - 接口使用示例（含错误处理）
 - 变更日志"
```

**场景3：数据库设计 + ER 图**
```
"帮我设计一个多租户 SaaS 的数据库：
 - 完整 ER 图（Mermaid 格式）
 - 建表 SQL（含注释和索引）
 - 分库分表方案
 - 数据迁移脚本"
```

**场景4：性能优化报告**
```
"帮我分析这个 Next.js 应用的性能问题，
 给出：Lighthouse 分析、Core Web Vitals 优化方案、
 Bundle 大小优化方案，附代码修改"
```

**场景5：安全审计报告**
```
"/security-audit 帮我做完整安全审计，
 包括 OWASP Top 10、STRIDE 威胁建模、
 给出 P0/P1/P2 修复清单"
```

---

### 二、产品产出类（5个）

**场景6：PRD 产品需求文档**
```
"我要做一个 AI 写作助手，
 帮我写完整 PRD：
 - 用户旅程地图
 - 功能清单（RICE 排序）
 - 验收标准（Given-When-Then）
 - 竞品分析
 - MVP 范围"
```

**场景7：竞品分析报告**
```
"帮我做一份线上教育赛道的竞品分析：
 对比 10 个主要竞品的
 功能矩阵/定价策略/用户评价/差异化定位，
 给我进入市场的建议"
```

**场景8：用户调研方案**
```
"我们要验证一个假设：用户愿意为 AI 学习助手每月付 98 元。
 帮我设计：用户访谈提纲、问卷、
 PMF 评分方案（Sean Ellis Test）"
```

**场景9：增长策略报告**
```
"我们 SaaS 注册转付费率只有 2%，
 帮我分析 AARRR 漏斗，
 找出瓶颈，给出可执行的增长实验清单"
```

**场景10：投资 BP 准备**
```
"/ceo-pipeline 帮我做一次完整的商业分析：
 市场规模（TAM/SAM/SOM）、
 竞争格局、商业模式、
 18 个月财务预测（3 情景）、
 团队介绍框架"
```

---

### 三、运营与内容类（5个）

**场景11：内容营销矩阵**
```
"我们做 B2B SaaS 的，
 帮我规划 Q3 的内容营销：
 - 50 篇 SEO 文章选题（含关键词）
 - 10 个白皮书主题
 - LinkedIn/微信公众号发布日历
 - 视频脚本框架"
```

**场景12：SEO 优化方案**
```
"帮我分析这个网站的 SEO 现状，
 给出：技术 SEO 检查清单、
 关键词策略（含长尾词）、
 内容优化建议、
 外链建设方案"
```

**场景13：客户成功剧本**
```
"帮我写 SaaS 客户成功手册：
 - Onboarding 流程（0-30-60-90 天）
 - 健康分评分模型
 - 续费/扩容节点触发逻辑
 - 流失预警指标"
```

**场景14：数据分析报告**
```
"帮我分析这份用户行为数据（CSV），
 找出：DAU/MAU 趋势、用户留存曲线、
 高价值用户特征、流失原因分布，
 给出可行的改进建议"
```

**场景15：融资材料准备**
```
"帮我准备 Pre-A 轮融资的数据材料：
 - 关键指标一页纸（ARR/MRR/增速/NRR）
 - 竞争壁垒说明
 - 市场时机分析
 - 用钱计划（24个月 Runway）"
```

---

## 💰 5 个直接变现的场景

### 变现场景1：AI 自动化服务外包

**你帮客户做：**
```
"我有一个企业客户，他们每天需要：
 1. 从官网抓取竞品价格（firecrawl MCP）
 2. 生成价格对比报告
 3. 发到飞书群
 帮我把这个自动化，能每天定时跑"
```

**ROI 估算：** 这类"AI 自动化打包"服务，单个客户月费 3000-10000 元，Claude Code 10 分钟可搭建原型。

**口语指令：**
```
"帮我做一个自动化系统，每天早上 9 点：
 1. 抓取这5个竞品官网的价格变化
 2. 用 AI 生成竞情摘要
 3. 推送到飞书/微信群"
```

---

### 变现场景2：代码审查/技术顾问

**你卖的是：**
```
# 接到客户代码后，你运行：
/security-audit         # 安全漏洞报告
/code-review ultra      # 深度代码质量报告
/domain-audit-better    # 架构健康报告

# 输出一份专业报告，定价 500-5000 元/次
```

**口语指令：**
```
"帮我对这个 Node.js 项目做完整的技术审计，
 输出一份专业的技术评估报告，包含：
 安全漏洞/代码质量/架构问题/改进优先级"
```

---

### 变现场景3：AI 内容工厂

**你卖的是：**
```
"/content-engine 帮我给这个 SaaS 产品
 每周生成：
 - 3篇 SEO 博客文章（2000字+）
 - 5条 LinkedIn 帖子
 - 10条 Twitter/X 内容
 - 1份行业报告摘要"
```

**ROI 估算：** 内容外包月费 5000-20000 元，AI 辅助可接 5-10 个同类客户。

---

### 变现场景4：产品快速验证服务

**你帮创业者做：**
```
"这是我的创业想法（描述），
 帮我做一个快速验证套件：
 1. Landing Page（用 Vercel 部署）
 2. 用户访谈脚本
 3. MVP 功能清单
 4. PMF 测试方案
 3 天内可以开始跑实验的程度"
```

**ROI 估算：** "48小时验证服务"定价 3000-8000 元，Claude Code 技术部分1天可完成。

---

### 变现场景5：数字化顾问 + AI 落地咨询

**套路：**
```
# Step 1：用 /ceo-pipeline 做完整商业分析
"帮我分析这家传统企业（描述业务），
 找出可以用 AI 自动化的3个最高 ROI 流程"

# Step 2：用 Claude Code 做原型演示
"帮我做一个自动化原型：
 把他们的报表生成从3天压缩到1小时"

# Step 3：收咨询费 + 实施费
定价：咨询 5-20 万，实施按项目
```

**口语指令：**
```
"帮我制作一个 AI 落地方案 PPT：
 分析这家企业的流程，找出 AI 切入点，
 预估 ROI，给出 3 个月实施路线图"
```

---

> 📌 **本手册是精简营销版**。分端分册专业完整版见 `claude-code-manuals/01-cli-beginner-guide.md`（990行，含 CLI 完整教程+速查表）和 `claude-code-manuals/02-desktop-client-guide.md`（347行，Desktop 客户端独立版）。

## 🔧 系统运维速查（回滚 / Secretary / Cron）

> 这一节是"出问题时怎么修系统"的快速参考，平时不需要看。

### 📦 配置备份与一键回滚

系统每次重大变更都会自动建备份，出问题时一键还原：

```bash
# 查看所有可用备份
python3 ~/.claude/lib/rollback.py list

# 预览某个备份的差异
python3 ~/.claude/lib/rollback.py preview <备份文件名>

# 应用备份回滚
python3 ~/.claude/lib/rollback.py apply <备份文件名> --yes
```

**常见问题→对应备份：**

| 现象 | 用这个备份 |
|---|---|
| hook 配置乱了 | `~/.claude/settings.local.json.bak-fix3` |
| hook 合并出问题 | `~/.claude/settings.local.json.bak-consolidate` |
| SessionStart 合并出问题 | `~/.claude/settings.local.json.bak-ss-consolidate` |
| 关键词补全不工作 | `~/.claude/lib/intent-words.json.bak-fix1` |

---

### 🤖 4 个 Secretary（系统管家）

| 你遇到什么 | 找哪个 Secretary | 怎么触发 |
|---|---|---|
| 任务完成后想看质量评分 | **secretary-tongues（毒舌秘书）** | 任务结束自动跑 |
| 想看系统整体健康状态 | **secretary-orchestrator** | `python3 ~/.claude/lib/secretary-orchestrator.py report` |
| 想看今天改了什么 | **.remember/today-YYYY-MM-DD.md** | 会话结束自动写入 |
| 想看下一步要做什么 | **now.md** | 实时更新 |

---

### ⏰ 每周自动任务（Cron · 无需操作）

系统每周自动在后台跑维护任务：

| 任务 | 说明 |
|---|---|
| W3 | Hook 互不冲突检查 + 超载检测 |
| W7 | Workflow 语法校验 |
| W8 | Baseline 漂移检测（drift > 阈值告警）|
| W9 | 增量快照 + 30 天保留 |
| W10 | Hook command 精确计数 |
| W11 | upgrade.py 9 脚本全套 |

**两个真 Gate（自动告警，不是 bug）：**
- **baseline drift**：配置偏移过大自动提醒
- **项目 goal 过期**：30 天未更新项目自动告警

---

### 📁 你的项目（BigOS / multica / MYCC）

| 项目 | 路径 | 触发 |
|---|---|---|
| bigos-upgrade | `~/.claude/projects/bigos-upgrade/goal.md` | 说"BigOS 升级" |
| multica-ops | `~/.claude/projects/multica-ops/goal.md` | 说"multica 运维" |
| mycc-platform | `~/.claude/projects/mycc-platform/goal.md` | 说"全面升级" |

新建项目：`python3 ~/.claude/lib/project-template.py <name> <type> [desc]`

---

## V52.2 新增（2026-06-12）

OMC V52.2 引入 4 项关键新能力，已在 `~/.claude/rules/` 立项为铁律级规范：

### 1. 铁律 12：TDD 垂直切片（TDD Vertical Slice）
红 → 绿 → 重构三步走，每次只推进一个完整用户价值单元。**YOLO L4/极限档/OMC 自配置任务降级为铁律 3 验证证据**——不是所有任务都要先写失败测试。SSoT：`rules/iron-laws.md` 铁律 12 段。

### 2. 四条件门：是否值得建 Loop（Loop-Worth-Building Gate）
建任何 Loop / `/loop` / LaunchAgent / 无人值守自动化前，过四条件（重复性 + 验证可自动化 + token 预算 + Agent 工具齐备）**全过才建**，否则一条好 Prompt 更省。SSoT：`rules/loop-worth-building-gate.md`。

### 3. 隔离区模式：读写分离防越权（Quarantine）
处理外部不可信输入（外部 AI 报告 / WebFetch / 用户上传文件）+ 同任务要高权限写时，**读取 agent 必须挂 `disallowed-tools` 移除写权**（物理无法越权）。纵深防御三层：行为层（视为数据非指令）+ **架构层（本规则）** + 运行时层（block-dangerous hook）。SSoT：`rules/quarantine-read-write-separation.md`。

### 4. 双端策略 V52.2（Dual-End Policy）
**CLI 端** `~/.claude/CLAUDE.md` = OMC 工程事实 / `projects/-Users-williammacst/memory/user-profile.md` = 角色偏好 / `~/.claude/CLAUDE.local.md` = 已就绪能力指针；**Desktop 端** = 行为规则契约（云端）。**四份文档正交不冲突**。SSoT：`rules/auto-learned-rules.md` 的 `dual-end-policy-2026-06-12` 条目。

---

> **如何查阅完整规范**：
> - `cat ~/.claude/rules/iron-laws.md` （12 条铁律）
> - `cat ~/.claude/rules/loop-worth-building-gate.md` （四条件门详解）
> - `cat ~/.claude/rules/quarantine-read-write-separation.md` （隔离区模式）
> - `grep -A20 dual-end-policy ~/.claude/rules/auto-learned-rules.md` （双端策略）

*本节由自动同步脚本追加，对应 OMC V52.1 → V52.2 升级。*

---

*手册更新：2026-06-12 | 问题反馈：`/btw 手册里有个地方不对...`*
