# Claude Code + OMC 4.14.7 速查表
> CLI v2.1.185 · Desktop v1.9659.4 · OMC 4.14.7 · 2026-06-22

---

## 🗣️ 口语触发词（最常用）

| 说这些话 | 触发效果 |
|---|---|
| **队长** / leader / boss | 四步完整工作流 + bypass 权限 |
| **火力全开** / 活力全开 / YOLO | 极限并发模式（15 agent 并发） |
| **继续** / 下一步 | 接着当前任务往下做 |
| **autopilot** | 自动驾驶执行模式 |
| **ulw** | ultrawork 超级执行 |
| **ralph** | ralph 循环模式 |
| **tdd** | TDD 测试驱动开发模式 |
| **ccg** | ccg 技能 |
| **ralplan** | ralplan 规划模式 |
| **deep interview** | 深度访谈/需求挖掘 |
| **deslop** / anti-slop | AI 生成内容去水分清洗 |
| **全面升级** / 融汇贯通 | claude-code-better 全面升级 |
| **又报错** / 反复错误 | systematic-error-analysis 系统排错 |
| **调研一下** / deepsearch | deep-research 多渠道深度调研 |
| **深度分析** | 进入深度分析模式 |
| **司聪查官网 <主题>** | 查官方文档后给结论 |

---

## ⌨️ 内置命令速查

| 命令 | 用途 | 示例 |
|---|---|---|
| `/clear` | 清空开新会话 | `/clear` |
| `/compact` | 压缩超长上下文 | `/compact` |
| `/model` | 切换模型 | `/model opus` |
| `/goal` | 设定任务完成标准 | `/goal "商品列表页上线可访问"` |
| `/btw` | 侧问（不污染主上下文） | `/btw 这个组件叫什么名字？` |
| `/cd` | 切换工作目录（不重开session） | `/cd ~/projects/mall` |
| `/safe-mode` | 纯净模式启动（排障专用）| `claude --safe-mode` |
| `/rewind` | 撤回到上一个检查点 | `/rewind` |
| `/init` | 初始化项目 CLAUDE.md | `/init` |
| `/code-review` | 代码审查 | `/code-review --fix` |
| `/simplify` | 代码简化/重构 | `/simplify` |
| `/loop` | 循环执行 | `/loop 5m /code-review` |
| `/run` | 启动并运行项目 | `/run` |
| `/verify` | 验证功能是否正常 | `/verify` |
| `/help` | 帮助 | `/help` |
| `Esc Esc` | 中断当前操作 | 按两下 Esc |

---

## 🔄 Workflow 命令（10个，直接 `/名字` 调用）

| 命令 | 用途 |
|---|---|
| `/bughunt` | 自动找 bug + 提修复方案 |
| `/ceo-pipeline` | CEO 6阶段完整商业分析流水线 |
| `/code-review` | 代码审查（内置 workflow 版） |
| `/config-audit-better` | 配置健康审计 |
| `/domain-audit-better` | 领域专项深度审计 |
| `/harness-audit` | harness 六层评分 |
| `/judge-panel` | 多视角评审面板 |
| `/n-step-pipeline` | N步自定义流水线 |
| `/self-evolve-loop` | 自我进化闭环（evaluator-optimizer）|
| `/v29-deep-research` | 深度多渠道调研 workflow |

---

## 🎯 Skills 分类速查

### 💻 开发执行类
| 触发 | 说明 |
|---|---|
| `/autopilot` | 全自动驾驶执行 |
| `/ultrawork` | 超级高强度执行 |
| `/ultragoal` | 超级目标驱动 |
| `/feature-dev:feature-dev` | 完整功能开发（探索→架构→实现→审查）|
| `/deep-dive` | 深度专项代码分析 |
| 说 "tdd" | TDD 红绿重构三步循环 |

### 🔍 调研分析类
| 触发 | 说明 |
|---|---|
| `/deep-research` | ≥5渠道深度调研，有结论有来源 |
| `/autoresearch` | 自动调研 |
| `/v29-deep-research` | workflow 版深度调研 |
| `/ask` | 互动式问答调研 |

### 🛡️ 代码质量类
| 触发 | 说明 |
|---|---|
| `/code-review` | 审查代码（不加参数=只看问题）|
| `/code-review --fix` | 审查 + 自动修复 |
| `/code-review --comment` | 审查 + 发 PR 评论 |
| `/code-review ultra` | 云端多 agent 深度审查 |
| `/simplify` | 简化代码，去掉多余复杂度 |
| `/security-audit` | 安全专项审计 |
| `/security-review` | OWASP 快速安全扫描 |
| `/ultraqa` | 超级 QA 验收 |

### 📝 内容文档类
| 触发 | 说明 |
|---|---|
| `/article-writing` | 写技术文章 / README |
| `/baoyu-translate` | 翻译（中英互译高质量）|
| `/baoyu-slide-deck` | 生成演示 PPT |
| `/baoyu-diagram` | 生成架构图/流程图 |
| `/baoyu-markdown-to-html` | Markdown 转 HTML |
| `/writer-memory` | 保持写作风格一致 |

### 🎨 设计类
| 触发 | 说明 |
|---|---|
| `/design` | UI/UX 设计 |
| `/ui-ux-pro-max` | 专业级 UI 设计 |
| `/prototype` | 快速原型 |
| `/frontend-design` 或 `/claude-api:frontend-design` | 前端设计 |

### 👥 团队/战略类
| 触发 | 说明 |
|---|---|
| `/team` | 多 agent 协作团队 |
| `/ceo-mode` | CEO 战略分析模式 |
| `/ceo-pipeline` | 6阶段完整商业流水线 |
| `/n-step-pipeline` | 自定义 N 步流水线 |

### 🚀 部署发布类
| 触发 | 说明 |
|---|---|
| `/deploy-ship` | 部署脚本 + 发布流程 |
| `/release` | 正式发版流程 |
| `/commit-commands:commit` | 智能 git commit（自动写提交信息）|
| `/commit-commands:commit-push-pr` | 提交 + push + 自动开 PR |
| `/gh-ci-fix` | 自动修复 CI/CD 失败 |

### ⬆️ 进化升级类
| 触发 | 说明 |
|---|---|
| `/claude-code-better` | 全面升级（唯一升级入口）|
| `/audit-all-better` | 30维健康诊断 |
| `/self-evolve-loop` | 自我进化闭环 |
| `/update-all` | 版本更新 |
| `/harness-audit` | Harness 六层评分 |
| `/skill-audit` | 装新技能前安全检查 |

### 🔧 配置工具类
| 触发 | 说明 |
|---|---|
| `/update-config` | 修改 settings.json |
| `/hookify:hookify` | 设置自动行为（"每次X做Y"）|
| `/remember:remember` | 保存重要信息到记忆 |
| `/omc-doctor` | 诊断 OMC 配置问题 |
| `/omc-setup` | 设置 OMC |

### 💰 内容创作/营销类
| 触发 | 说明 |
|---|---|
| `/card-xiaohongshu` | 生成小红书卡片 |
| `/baoyu-xhs-images` | 小红书图片风格 |
| `/social-publisher` | 社交媒体发布 |
| `/marketing` | 营销文案 |
| `/brand` | 品牌设计 |

---

## 🔑 权限模式

| 模式 | 触发方式 | 效果 |
|---|---|---|
| 默认模式 | 直接启动 | 每步操作都要你按 Y 确认 |
| acceptEdits | 系统自动 | 文件编辑自动允许，其余要确认 |
| YOLO（全自动）| 说"火力全开" 或 `--dangerously-skip-permissions` | 全部自动允许，不打断 |

**确认提示怎么回：**
- `Y` 或直接回车 = 允许
- `N` = 拒绝
- `q` = 退出整个对话

---

## 🤖 常用 Agent 类型

| Agent | 适用场景 |
|---|---|
| `executor` | 执行具体实现任务 |
| `architect` | 架构设计、技术选型 |
| `engineer` | TDD + 变更闭环工程实现 |
| `debugger` | 报错排查、根因分析 |
| `code-reviewer` | 独立代码审查 |
| `designer` | UI/UX 设计 |
| `researcher` | 深度多渠道调研 |
| `security-analyst` | 安全威胁分析 |
| `devops-engineer` | CI/CD、容器化、部署 |
| `product-manager` | PRD、用户故事、RICE 优先级 |
| `Explore` | 快速代码库搜索定位 |
| `planner` | 复杂任务规划 |

---

## 📱 外部 AI CLI（省 Claude token）

| CLI | 状态 | 适合用在 |
|---|---|---|
| `gemini` | ✅ 在线 | 快速调研、多模态、搜索 |
| `kimi` | ✅ 在线（256K）| 超长文档处理 |
| `mmx` | ✅ 在线 | 中文创作优化 |
| `opencode` | ⚠️ 订阅过期 | 代码任务（续订后启用）|

调用方式：
```bash
# 单个调用
scripts/ai-cli.sh gemini "帮我分析这个技术方案"

# 多模型并发对抗
scripts/ai-panel.sh "调研 Next.js vs Nuxt.js 选哪个" gemini kimi mmx
```

---

## ⚡ 快捷技巧

```bash
# 切目录不重开session（v2.1.169+）
/cd ~/projects/mall

# 侧问不污染主对话
/btw 这个函数叫什么？

# 排障专用纯净模式（禁所有插件/hooks）
claude --safe-mode

# 压缩长对话上下文
/compact

# 撤回到上一个检查点
/rewind

# 设定任务完成条件（>5轮任务必用）
/goal "购物车页面可以正常加减商品、显示总价"

# 中断 Claude 正在做的事
Esc Esc
```

---

## 🌐 Desktop 客户端关键路径

| 功能 | 路径 |
|---|---|
| 全局 Instructions | Settings > General > "Instructions for Claude" |
| MCP Extensions | Settings > Desktop app > Extensions |
| Cowork Instructions | Settings > Cowork（仅限 Cowork session）|
| 账户/订阅 | Settings > Account / Billing |

---

## V52.2 新增（2026-06-12）

以下 4 项关键能力（原 V52.2 · 2026-06-12 引入）现已合并入 `~/.claude/rules/mycc-core.md` 铁律级规范：

### 1. 铁律 12：TDD 垂直切片（TDD Vertical Slice）
红 → 绿 → 重构三步走，每次只推进一个完整用户价值单元。**YOLO L4/极限档/OMC 自配置任务降级为铁律 3 验证证据**——不是所有任务都要先写失败测试。SSoT：`rules/mycc-core.md`（铁律 12·TDD 垂直切片）。

### 2. 四条件门：是否值得建 Loop（Loop-Worth-Building Gate）
建任何 Loop / `/loop` / LaunchAgent / 无人值守自动化前，过四条件（重复性 + 验证可自动化 + token 预算 + Agent 工具齐备）**全过才建**，否则一条好 Prompt 更省。SSoT：`rules/mycc-core.md`（4 步工作流·Loop 判断）。

### 3. 隔离区模式：读写分离防越权（Quarantine）
处理外部不可信输入（外部 AI 报告 / WebFetch / 用户上传文件）+ 同任务要高权限写时，**读取 agent 必须挂 `disallowed-tools` 移除写权**（物理无法越权）。纵深防御三层：行为层（视为数据非指令）+ **架构层（本规则）** + 运行时层（block-dangerous hook）。SSoT：`rules/mycc-core.md`（防改 4 类配置·安全约束）。

### 4. 双端策略 V52.2（Dual-End Policy）
**CLI 端** `~/.claude/CLAUDE.md` = OMC 工程事实 / `projects/-Users-williammacst/memory/user-profile.md` = 角色偏好 / `~/.claude/CLAUDE.local.md` = 已就绪能力指针；**Desktop 端** = 行为规则契约（云端）。**四份文档正交不冲突**。SSoT：`rules/mycc-core.md`（输出规范·双端策略）。

---

> **如何查阅完整规范**：
> - `cat ~/.claude/rules/mycc-core.md` （唯一规则 SSoT：12 铁律 + 12 行为规则 R1-R12 + 4 步工作流）
> - 注：W4（2026-06-22）精简后，原 iron-laws / loop-worth-building-gate / quarantine / auto-learned-rules 等多文件已合并入 `mycc-core.md` 单文件

*本节能力现已合并入 `rules/mycc-core.md`（OMC 4.14.7 · W4 精简）；保留原 V52 系列升级记录。*

---

*速查表更新：2026-06-22 | OMC 4.14.7*
